# API Quickstart

1. Criar ambiente virtual.
2. Instalar `requirements.txt`.
3. Copiar `.env.example` para `.env`.
4. Configurar `JURISFLOW_DATABASE_URL` ou variaveis discretas.
5. Aplicar `python database/apply_schema.py`.
6. Executar `python app.py`.
7. Validar `python tests/smoke_api.py`.
